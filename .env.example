# On Linux, run `id -u` and `id -g` to get these values.
PUID=1000
PGID=1000
TZ=America/New_York
# Published image. Replace YOUR-GITHUB-USERNAME after creating the repository.
RECOMMENDARR_IMAGE=ghcr.io/YOUR-GITHUB-USERNAME/recommendarr:latest
